const exampleData = (state) => state.exampleDetails.data;
const exampleSuccessMessage = (state) => state.exampleDetails.successMessage;
const exampleErrorMessage = (state) => state.exampleDetails.errorMessage;
const exampleLoading = (state) => state.exampleDetails.loading;

export default {
  exampleData,
  exampleSuccessMessage,
  exampleErrorMessage,
  exampleLoading,
};
